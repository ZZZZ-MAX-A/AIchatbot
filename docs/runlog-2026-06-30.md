# 2026-06-30 工作日志

日期：2026-06-30

用途：记录今天 RAG 边界修正和 QQ 侧 MemoryRAG 调试命令开发的实际进度。后续继续工作时，先读这份文档，避免把 ProjectDocRAG 又误接进 QQ。

## 当前结论

今天用户明确纠正了 RAG 边界：

```text
QQ 侧只做 MemoryRAG。
QQ 侧 MemoryRAG 只摘录长期事实记忆、长期偏好记忆和正式会话摘要。
ProjectDocRAG 不出现在 QQ 指令里，也不进入 QQ 普通聊天上下文。
ProjectDocRAG 放在 QQ 外，用于本地脚本、开发侧工具、后续 Codex / MainAgentGraph 恢复项目上下文。
```

这个边界已经同步修正到设计文档，但代码实现只刚开始，今天不要继续扩展。

## 今日已完成

### 1. 修正 v1.4 RAG 设计文档边界

修改文件：

```text
docs/v1.4-memory-rag.md
```

已修正内容：

```text
ProjectDocRAG 从 QQ 调试命令中移除。
ProjectDocRAG 明确只走 QQ 外入口。
QQ 侧调试命令只保留 MemoryRAG：
  /RAG状态
  /记忆检索 查询内容
  /重建记忆索引
明确不在 QQ 侧提供：
  /项目文档检索
  /重建项目文档索引
  /查看召回 中混入 project_docs
普通聊天注入格式中移除了项目文档示例。
上下文组装位置中移除了 RAG 项目文档注入。
Combined Retrieval 改为 QQ 外开发侧能力。
MainAgentGraph 相关表述改为 QQ 外 MainAgentGraph / 开发侧工具。
阶段路线中，项目文档查询改为 QQ 外脚本，不再是 QQ 命令。
```

文档里的最终边界现在是：

```text
MemoryRAG：QQ 侧，长期事实记忆 + 长期偏好记忆 + 正式会话摘要。
ProjectDocRAG：QQ 外，本地脚本 / 开发侧工具 / 后续 Codex 或 MainAgentGraph 使用。
```

### 2. 开始写 MemoryRetrievalGraphRunner 骨架

新增文件：

```text
src/plugins/ai_chat/graph/retrieval.py
```

当前只完成了纯 graph 边界骨架：

```text
MemoryRetrievalAction:
  STATUS
  QUERY
  REBUILD

MemoryRetrievalNode:
  VALIDATE_RETRIEVAL_REQUEST
  EXECUTE_RETRIEVAL_OPERATION
  RENDER_RETRIEVAL_REPLY

MEMORY_RETRIEVAL_NODE_SEQUENCE

MemoryRetrievalState
MemoryRetrievalGraphResult
MemoryRetrievalGraphExecution
MemoryRetrievalGraphRunner
```

这个文件目前只是 scaffold，尚未接入：

```text
src/plugins/ai_chat/graph/__init__.py
src/plugins/ai_chat/__init__.py
QQ on_command
测试加载器 pure_ai_chat_loader
单元测试
```

也尚未实现实际命令逻辑：

```text
/RAG状态
/记忆检索 查询内容
/重建记忆索引
```

## 当前未完成状态

当前工作区有未提交改动：

```text
M  docs/v1.4-memory-rag.md
?? src/plugins/ai_chat/graph/retrieval.py
?? docs/runlog-2026-06-30.md
```

注意：本日志文件本身也是未提交新增文件。

今天没有提交 commit。

## 验证情况

在新增 `src/plugins/ai_chat/graph/retrieval.py` 之前，已执行：

```powershell
git diff --check
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：

```text
Ran 139 tests in 2.235s
OK
```

新增 `retrieval.py` 之后，因为用户要求今天停止，尚未重新跑测试。明天继续时需要先跑：

```powershell
git diff --check
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

## 明天继续建议

明天不要从 ProjectDocRAG QQ 命令开始。正确下一步是继续 QQ 侧 MemoryRAG：

```text
1. 把 src/plugins/ai_chat/graph/retrieval.py 导出到 graph/__init__.py。
2. 在 tests/pure_ai_chat_loader.py 加载 retrieval graph 模块。
3. 给 MemoryRetrievalGraphRunner 增加 graph contract / runner 测试。
4. 在 src/plugins/ai_chat/__init__.py 注册仅 MemoryRAG 的 QQ 命令：
   /RAG状态
   /记忆检索 查询内容
   /重建记忆索引
5. 实现 run_memory_retrieval_graph(event, action, query="")。
6. /记忆检索 只调用 rag.memory_index.retrieve_memory，不调用 retrieve_project_docs。
7. /重建记忆索引 只调用 rag.memory_index.rebuild_memory_rag_index。
8. /RAG状态 显示 RAG 开关、embedding 配置、rag_documents / rag_embeddings 数量、source_type 统计和最近错误。
9. 补测试，确认 QQ 侧不会注册项目文档相关命令。
```

禁止误做：

```text
不要新增 /项目文档检索。
不要新增 /重建项目文档索引。
不要在 QQ /查看召回 中混入 project_docs。
不要把 ProjectDocRAG 注入普通聊天上下文。
```

## 当前阶段一句话

RAG 底层索引已经有基础，ProjectDocRAG 和 MemoryRAG 的设计边界今天已重新收紧；QQ 侧 MemoryRAG 调试命令的 graph scaffold 刚开始写，尚未接线、尚未测试、尚未提交。
